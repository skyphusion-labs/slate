# Dischord fleet stack: Slate (Vivijure Screenwriter's Assistant)
#
# Secrets (never committed): create ~/dev/slate/stacks/.env with:
#   DISCORD_TOKEN              - Discord bot token
#   CF_ACCESS_CLIENT_ID        - CF Access service token client ID
#   CF_ACCESS_CLIENT_SECRET    - CF Access service token secret
#   CF_D1_TOKEN                - CF API token with D1:Write permission
#   CF_AIG_TOKEN               - CF API token for AI Gateway (Anthropic path)
#   CF_GATEWAY_ENDPOINT        - CF AI Gateway compat URL
#   SEARCH_WORKER_URL          - vivijure-search Worker base URL
#   SEARCH_SECRET              - shared secret for X-Search-Secret header
#   CF_R2_ACCESS_ID            - R2 access key (for portrait uploads via LLM API)
#   CF_R2_SECRET               - R2 secret key
#
# Claude backend (default): set CF_AIG_TOKEN + CF_GATEWAY_ENDPOINT, DISCORD_MODEL=claude-sonnet-4-6
# Ollama backend:            unset CF_AIG_TOKEN, set OLLAMA_BASE_URL + DISCORD_MODEL=<ollama model>
#
# Deploy:   ssh root@dischord.internal "cd ~/dev/slate/stacks && docker compose -p slate -f dischord.yml up -d"
# Redeploy: ssh root@dischord.internal "cd ~/dev/slate/stacks && docker compose -p slate -f dischord.yml up -d --force-recreate slate"
# Logs:     ssh root@dischord.internal "docker compose -p slate -f ~/dev/slate/stacks/dischord.yml logs -f"

services:
  slate:
    image: node:24
    restart: unless-stopped
    working_dir: /app
    command: sh -c "npm ci --silent && node bot.mjs"
    volumes:
      - /root/dev/slate:/app
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      DISCORD_CHANNEL_IDS: "1513838828991811616"
      DISCORD_MODEL: ${DISCORD_MODEL:-claude-sonnet-4-6}
      DISCORD_LOG: /dev/stdout
      VIVIJURE_API_URL: https://vivijure.skyphusion.org
      LLM_API_URL: https://play.skyphusion.org
      CF_ACCESS_CLIENT_ID: ${CF_ACCESS_CLIENT_ID}
      CF_ACCESS_CLIENT_SECRET: ${CF_ACCESS_CLIENT_SECRET}
      CF_D1_TOKEN: ${CF_D1_TOKEN}
      CF_D1_ACCOUNT_ID: fabcb25d9c7eb087110ec474a03e50d2
      CF_D1_DATABASE_ID: faac1698-5ffe-4f0e-8147-761c0747e957
      CF_AIG_TOKEN: ${CF_AIG_TOKEN:-}
      CF_GATEWAY_ENDPOINT: ${CF_GATEWAY_ENDPOINT:-}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://wendy.internal:11434/v1}
      SEARCH_WORKER_URL: ${SEARCH_WORKER_URL:-}
      SEARCH_SECRET: ${SEARCH_SECRET:-}
      TRUSTED_BOT_IDS: ${TRUSTED_BOT_IDS:-}
      LOG_WORKER_URL: ${LOG_WORKER_URL:-}
      LOG_SECRET: ${LOG_SECRET:-}
      LOG_SERVICE: ${LOG_SERVICE:-slate}
